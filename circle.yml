machine:
  timezone:
    Asia/Tokyo

dependencies:
    post:
        - aws configure set region ap-northeast-1

test:
  override:
    - echo "Nothing to do here"

deployment:
  production: # just a label; label names are completely up to you
    branch: master
    commands:
      - aws s3 sync / s3://thirtylimits-form/ --delete
  staging:
    branch: staging
    commands:
      - aws s3 sync / s3://st-thirtylimits-form/ --delete